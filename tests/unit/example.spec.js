// import { shallowMount } from '@vue/test-utils';
// import DiagramEditor from '@/components/DiagramEditor.vue';

describe('DiagramEditor.vue', () => {
  it('not implemented', () => {
    // const wrapper = shallowMount(DiagramEditor);
    // expect(wrapper.text()).toMatch(msg);
  });
});
